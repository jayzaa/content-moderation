# Go build artifacts
/bin/
/dist/
*.exe
*.test
*.out

# Go workspace file (local only)
go.work
go.work.sum

# Local secrets / credentials — never commit real values
.env
*.pem
*.p12
service-account*.json
credentials*.json
credentials*.csv
/secrets/

# Runtime-generated data — not part of the source tree
/logs/

# Kiro CLI session state, skills, and memory docs — local tooling only,
# not part of the public project
.kiro/
